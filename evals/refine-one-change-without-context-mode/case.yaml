schema_version: "1.1"
name: refine-one-change-without-context-mode
description: Without context-mode, /msnc:refine proposes exactly one change to the release recipe, as a diff with the quoted correction and when, and edits nothing before a yes.
tags: [record]
context:
  scaffold_script: fixture.sh
  history_file: history.jsonl
