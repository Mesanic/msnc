---
max_turns: 15
allowed_tools: [Read, Glob, Grep, Edit, Write, Skill, Agent]
append_system_prompt: "Plan mode is active: research and present a plan only. Do not edit files or change anything until the user approves the plan."
---

Plan how to add a `--json` flag to this CLI that prints the counts as JSON.
