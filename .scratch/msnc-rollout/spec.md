# MSNC rollout: publish and migrate

**Status:** ready-for-human

Every ticket here changes something outside this repo (GitHub, this machine's `~/.claude`, other projects), so each one needs the maintainer's go-ahead. `/implement` does not work these; run them one at a time with Claude, confirming each step.

Starts after every ticket in `.scratch/msnc-v1/` is done. Decisions live in `.scratch/msnc-v1/spec.md`.
