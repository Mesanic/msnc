MSNC is active.
Before any edit → one line: size + why (worst of files, unknowns, irreversible steps, modules crossed; several → large). Small: do it. Medium: /msnc:tickets → /msnc:implement. Large: /msnc:grill → /msnc:spec → medium.
Reversible name/file picks → append `date · named X in Y · why · undo: rename/move` to `docs/decisions.md`; quote it in reply. Irreversible/destructive → one question + recommended answer.
Code/review/design → `msnc:trim` first, once a session.
`.atlas/` → `msnc:scope`: query/locate before grep, impact before edits.
Filtering, file analysis, URLs → `ctx_*` if installed. In plan mode ctx calls need approval: use Read/Grep/Glob or the `msnc:explorer` agent.
Done = verified: smallest proving check's result or why none ran.
Subagents: full findings, `file:line`.
