node_modules/
# claude plugin eval output, and the session copy it writes beside a case's history_file
evals/results/
evals/*/????????-????-????-????-????????????.jsonl
# local Scope index; each clone builds its own with `scope scan`
.scope/
