# Decisions

- 2026-09-24 · named Scope's old CLI, atlas and scalpel → Scope everywhere (`scope impact`, `SCOPE_*`, `.scope/files/` + `.scope/symbols/`, `engine/files` + `engine/symbols`) and the old repo → `Mesanic/msnc-scope` · one name instead of four; the repo has 0 stars and 0 forks; `msnc-scope` stays findable on GitHub where plain `scope` wouldn't · undo: rename back (GitHub redirects the old repo URL)
- 2026-09-24 · named the new home for the local `Map v2` folder `Mesanic/map-v2` (private, archived) · matches `roundwell-v2`; Map v2 had no remote, so its 43 commits existed only on this machine · undo: `gh repo unarchive Mesanic/map-v2`, rename or delete it
- 2026-09-24 · MSNC's `skills/scope` becomes the only source of Scope; the old repo (now `Mesanic/msnc-scope`) is archived (supersedes the `msnc-scope` repo half of the first line) · no one else uses it, so one continuously developed copy beats keeping two in sync · undo: unarchive `Mesanic/msnc-scope` and re-add its `vendor.json` entry
- 2026-09-24 · no aliases for the old CLI's names (its script, its env-var prefix, its `impact` call in the gate) · the only user is you, and every caller migrates in the same pass · undo: add a forwarding script under the old name and env fallbacks
- 2026-09-24 · the four indexed repos move `.atlas/` + `.map/` to `.scope/` by a one-time `git mv`, no migration code in the engine · four repos, once; keeps committed summaries and notes · undo: `git revert` the move commit in each repo
- 2026-09-24 · commit rollout ticket 04 in each repo before the rename, as its own commit · mixing the two makes both hard to review or revert · undo: `git revert`
- 2026-09-24 · rename everything that names the tool: folders, files (`atlas.mjs`, `map.mjs`), identifiers (`atlasDir`), comments, tests; doctor's legacy detection keeps the old names · a half-rename leaves four names in the code · undo: `git revert` the rename PR
- 2026-09-24 · one map at `.scope/MAP.md` · the file both engines point to, at the top of the data folder · undo: move it back to `.scope/files/MAP.md`
- 2026-09-24 · no version bump, no migration note, no upstream tag: MSNC stays 0.1.0 · no one else uses MSNC yet · undo: bump when there are other users
- 2026-09-24 · the old repo is renamed to `Mesanic/msnc-scope`, then archived · the name matches the tool's new home · undo: unarchive, rename back
- 2026-09-24 · drop the old repo's copy bookkeeping: its `vendor.json` entry, `skills/scope/UPSTREAM.md`, `skills/scope/LICENSE`, its `THIRD_PARTY_NOTICES.md` section and README credits row; keep `skills/scope/THIRD-PARTY-NOTICES.md` (tree-sitter); README names the archived repo Scope began as · MSNC is the only source now, same owner and license · undo: `git revert`
- 2026-09-24 · commit ticket 04 and the rename on each repo's current branch (Maji `master`, Roundwell `phase-1-termination-reopen`, Roundwell v2 `main`, Hackathon `claude/audit-remediation`) · no branch switching with 84 uncommitted files; default branches get it when those merge · undo: `git revert`
- 2026-09-24 · push those commits; Roundwell v2 stays local (no remote) · the repos' remotes stay current · undo: `git revert` and push
- 2026-09-24 · ticket 04: remove the `<!-- atlas:begin -->` blocks in Maji, Roundwell and Roundwell v2 in the ticket-04 commit; drop Map v2 (archived) and mark the ticket done · Tuner owns that routing · undo: `git revert`
- 2026-09-24 · keep both backup folders until the four repos run `scope impact`; then the user deletes them · deletion is the user's call · undo: none needed
- 2026-09-24 · track `.scope/symbols/config.json` (was the untracked, hand-written `.map/config.json` in Maji and Roundwell v2) · hand-written settings, like the already-tracked file-graph config · undo: `git rm --cached`
- 2026-09-24 · before archiving, push a README pointer from `Mesanic/msnc-scope` to `Mesanic/msnc` · an archived repo is read-only and its README still describes a live tool · undo: unarchive and revert the README
- 2026-09-24 · leave past `evals/RESULTS.md` entries with the old names; new entries use Scope · they record what actually ran · undo: rewrite them
- 2026-09-24 · order: one MSNC rename PR (pure `git mv` commit, then content edits, then bookkeeping removal); after merge, each repo gets its ticket-04 commit, its move commit and a `scope impact` check; the GitHub rename and archive come last · git history follows the files, and nothing references a read-only repo · undo: `git revert` per step
- 2026-09-24 · no separate spec: `/msnc:tickets` works from this decision log · a spec would restate it · undo: run `/msnc:spec`
- 2026-09-24 · engine override env vars are `SCOPE_FILES` and `SCOPE_SYMBOLS` (were the old CLI's `*_ATLAS` and `*_SCALPEL`) · they name the engines · undo: rename them
- 2026-09-24 · `scope.mjs` finds its engines only in the bundled `engine/`; the old sibling and `.claude/skills` fallbacks are gone · no aliases for old layouts · undo: re-add the fallback paths
- 2026-09-24 · identifiers renamed: `atlasDir`→`filesDir`, `AtlasError`→`ScopeError`, `mergeIntoAtlas`→`mergeIntoFileGraph`, `mapDirFor`→`symbolsDirFor` (`.scope/symbols`), and the rest that named the tool · they name the tool · undo: rename them
- 2026-09-24 · in-memory provenance `atlas`/`scalpel` → `files`/`symbols` in the merge and the viewer together; stored tool ID `scope-symbols@0.0.1`; viewer `view/scope.html`; placeholders `__SCOPE_DATA__`, `__SCOPE_SYMBOLS_PAYLOAD__` · display-only names that must match each other · undo: rename both sides
- 2026-09-24 · the symbols engine skips its `.gitignore` line when the repo already ignores all of `.scope/` · same rule the files engine applies · undo: check `.scope/symbols` only
- 2026-09-24 · deleted the files engine's never-printed CLAUDE.md block, and fixed two self-manifest summaries that claimed init writes a CLAUDE.md block and a hook · simpler than renaming dead text; the summaries were wrong · undo: `git revert`
- 2026-09-24 · the symbols engine's messages say `scope <cmd>`, and `symbols.mjs init` for commands only the engine has · `map` was that CLI's old name · undo: rename them
- 2026-09-24 · the old repo's removed notices section became a `## tree-sitter` pointer to `skills/scope/THIRD-PARTY-NOTICES.md` · the root notices still lead to the bundled grammars' notices · undo: delete that section
- 2026-09-24 · deleted the symbols engine's printed pre-commit `HOOK_SNIPPET` rather than resolving its path · `scope scan` never shows that init output, and MSNC's gate replaces it · undo: restore it with a path built from `import.meta.url`
- 2026-09-24 · deleted `scope.mjs`'s dead CLAUDE.md-block and hook-install code and its `--no-hook`/`--no-claude-md` flags · they could never run under MSNC, and they wired hooks MSNC doesn't ship · undo: `git revert`
- 2026-09-24 · the symbols engine also gitignores `.scope/symbols/view-data.html` · `git add .scope` would otherwise commit a generated viewer · undo: drop that line from init
- 2026-09-24 · doctor's migration recipe uses `mkdir -p .scope`, `git mv` for `.atlas`, plain `mv` for `.map` (plus `git rm -r --cached .map` when it tracked files), `perl -pi` rewrites of the config ignore rule and `MAP.md` markers, and swaps the `.gitignore` lines · verified in a temp repo; `git mv` fails without the parent folder and on an untracked `.map/` · undo: edit that doc block
- 2026-09-24 · the `atlas:begin` blocks in Maji, Roundwell and Roundwell v2 became `## Scope records` sections keeping their project rules (graph and issues are the only stores, write-back timing, Roundwell's founder-approval rule), not deleted (supersedes that part of the ticket-04 line) · those rules are the user's, not Scope plumbing · undo: delete the sections
- 2026-09-25 · replaced the archived `Mesanic/msnc-scope` contents with a snapshot of MSNC's `skills/scope` at `396c6f5` (commit `a71bcd1`, earlier history kept), then re-archived · the repo's code still used the old names · undo: unarchive, `git revert a71bcd1`, archive
- 2026-09-25 · removed Scope's old CLI name from every current MSNC file: past `.scratch/` and `evals/RESULTS.md` entries now use Scope's names (supersedes the line above about leaving past eval entries), doctor no longer checks for that old install, and the README calls the origin "a standalone repo, now an archived snapshot at `Mesanic/msnc-scope`" · the old name suggested a link to an unrelated package of the same name · undo: `git revert`
- 2026-09-25 · rewrote `Mesanic/msnc-scope`'s history to one commit, `1941cfc` (the snapshot, with no mention of Scope's old CLI name), force-pushed and re-archived; the 8 earlier commits are backed up in `~/.claude/backups/msnc-scope-history-2026-09-25/msnc-scope.bundle` · the old history kept the old name visible to anyone browsing the repo · undo: unarchive, force-push the bundle's `main`, archive
- 2026-09-25 · named the feature folder `.scratch/context-mode-wait/` and put the ctx wait line in `hooks/msnc.mjs`, not the Tuner · the Tuner is at 799/800 characters and the line only matters when context-mode is enabled · undo: move the folder, drop the hook line
- 2026-09-25 · the ctx wait line counts context-mode as enabled only when a `context-mode@*` key in `enabledPlugins` is exactly `true` (user, then project, then local settings; managed settings and `--settings` not read) · matches what Claude Code writes; the ticket named those three files · undo: edit `ctxEnabled` in `hooks/msnc.mjs`
- 2026-09-25 · `test/dispatcher.test.mjs` runs every hook with an empty config dir, home and cwd · real user settings leaked in and broke exact-output tests · undo: revert the `baseEnv` and `run()` changes
