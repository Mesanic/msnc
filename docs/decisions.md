# Decisions

- 2026-09-24 · named sextant, atlas and scalpel → Scope everywhere (`scope impact`, `SCOPE_*`, `.scope/files/` + `.scope/symbols/`, `engine/files` + `engine/symbols`) and the repo `Mesanic/sextant` → `Mesanic/msnc-scope` · one name instead of four; `sextant` on npm is Matt Pocock's; the repo has 0 stars and 0 forks; `msnc-scope` stays findable on GitHub where plain `scope` wouldn't · undo: rename back (GitHub redirects the old repo URL)
- 2026-09-24 · named the new home for the local `Map v2` folder `Mesanic/map-v2` (private, archived) · matches `roundwell-v2`; Map v2 had no remote, so its 43 commits existed only on this machine · undo: `gh repo unarchive Mesanic/map-v2`, rename or delete it
